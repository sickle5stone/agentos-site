services:
  - type: web
    name: agentos-site
    runtime: static
    staticPublishPath: .
    buildCommand: "true"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
